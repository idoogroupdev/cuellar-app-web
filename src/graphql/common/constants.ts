export const PAGE_SIZE = 10;

export const DEFAULT_PAGINATION = {
  edges: [],
  pagination: {
    totalCount: 0,
    currentPage: 1,
    totalPages: 0,
    hasPreviousPage: false,
    hasNextPage: false,
  },
};
